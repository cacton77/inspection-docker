# System build dependencies
build-essential
cmake
git
python3-colcon-common-extensions
python3-colcon-mixin
python3-colcon-override-check
python3-flake8
python3-rosdep
python3-setuptools
python3-vcstool
python3-pip
wget
neovim

# Graphics and GUI dependencies
freeglut3-dev
libgl1-mesa-dev
libglu1-mesa-dev
libxrandr-dev
libxinerama-dev
libxcursor-dev
libxi-dev

# ROS Jazzy packages
# NOTE: Most MoveIt build deps removed — apt MoveIt pulls them automatically.
# Kept user-workspace deps (ros2_control, control, testing utilities).
ros-jazzy-topic-tools
ros-jazzy-diagnostic-updater
ros-jazzy-ros2-control*
ros-jazzy-control*
ros-jazzy-ros-testing
ros-jazzy-launch-param-builder
ros-jazzy-generate-parameter-library
ros-jazzy-object-recognition-msgs
ros-jazzy-urdfdom-py
ros-jazzy-foxglove-bridge
python3-pykdl
